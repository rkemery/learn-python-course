# create tuple - tuples are immutable
# 4 is at index position 0
# a list is doing the same thing
# used for data that never changes
coordinates = (4, 5), (6, 7), (80, 34)
print(coordinates[0])
print(coordinates[1])
